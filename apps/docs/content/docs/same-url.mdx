---
title: Same URL
description: Used to detect whether the starting URL is the same as the target URL
---

## `isSameURL()`

### Import

```ts
import { isSameURL } from '@lop-town/bprogress-react';
```

### Type

```ts
function isSameURL(target: URL, current: URL): boolean;
```

## `isSameURLWithoutSearch()`

### Import

```ts
import { isSameURLWithoutSearch } from '@lop-town/bprogress-react';
```

### Type

```ts
function isSameURLWithoutSearch(target: URL, current: URL): boolean;
```
